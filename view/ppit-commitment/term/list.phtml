<table class="table table-striped ppit_index">
	<tr>
		<th>&nbsp;</th>

<?php foreach ($context->getConfig('commitmentTerm/list') as $propertyId => $rendering) : ?>
		<th align="center">
	<?php $label = $context->getConfig('commitmentTerm')['properties'][$propertyId]['labels'][$context->getLocale()] ?>
	<?php if ($label) : ?>
			<button type="button" class="btn btn-default btn-xs ppit-sort-anchor" id="<?php echo $propertyId ?>-anchor">
				<span><?php echo $label ?></span>
<?php if ($major == $propertyId) : ?>
				<span class="glyphicon glyphicon-triangle-<?php echo ($dir == 'ASC') ? 'top' : 'bottom' ?>"></span>
<?php endif ?>
			</button>
	<?php endif;?>
		</th>
<?php endforeach;?>

		<th>&nbsp;</th>
	<tr>
		<td style="text-align: center">
			<button type="button" class="btn btn-default index-btn term-detail-anchor" title="<?php echo $this->translate('Add', 'ppit-core', $context->getLocale())?>" id="term-detail-anchor_0">
				<span class="glyphicon glyphicon-plus" id="add-glyphicon"></span>
			</button>
		</td>
		
		<td colspan="7">&nbsp;</td>
	</tr>

<?php $i = 0; foreach ($terms as $term) : ?>
	
	<tr class="term">
	
		<td style="text-align: center">
			<button type="button" class="btn btn-default index-btn term-detail-anchor" title="<?php echo $this->translate('Detail', 'ppit-core', $context->getLocale())?>" id="term-detail-anchor_<?php echo $term->id ?>">
				<span class="glyphicon glyphicon-zoom-in" id="<?php echo $term->id ?>-glyphicon"></span>
			</button>
		</td>

	<?php foreach ($context->getConfig('commitmentTerm/list') as $propertyId => $rendering) : ?>
	
		<?php if ($propertyId == 'name') : ?>
		<td><?php echo $term->name ?></td>

		<?php elseif ($rendering == 'date') : ?>
		<td><?php echo $context->decodeDate($term->properties[$propertyId]) ?></td>

		<?php elseif ($rendering == 'number') : ?>
		<td style="text-align: right"><?php echo $context->formatFloat($term->properties[$propertyId], 2) ?></td>

		<?php elseif ($rendering =='select') : ?>
			<?php $property = $context->getConfig('commitmentTerm')['properties'][$propertyId] ?>
		<td><?php echo $property['modalities'][$term->properties[$propertyId]][$context->getLocale()] ?></td>

		<?php else : ?>
		<td><?php echo $term->properties[$propertyId] ?></td>

		<?php endif;?>
		
	<?php endforeach;?>
	
		<td>
	<?php if ($term->document) : ?>
			<?php $dropbox = $context->getConfig('ppitDocument')['dropbox'] ?>
			<a class="glyphicon glyphicon-paperclip" href="<?php echo $dropbox['root'].'/'.$dropbox['folders']['settlements'].'/'.$term->document ?>" title="<?php echo $this->translate('Attachment', 'ppit-document', $context->getLocale()) ?>" target=".blank" />
	<?php endif;?>
		</td>
	</tr>
	
<?php $i++; endforeach; ?>

</table>
